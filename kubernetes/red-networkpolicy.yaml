apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: red
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/red: "true"
  podSelector:
    matchLabels:
      io.kompose.network/red: "true"
